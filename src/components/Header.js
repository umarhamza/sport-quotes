import React from 'react';

const Header = props => {

    return (
        <header id="header" className="header">
            <div className="container">
                <h1 className="header--title">Sporting Quotes</h1>
            </div>
        </header>
    );

}

export default Header;